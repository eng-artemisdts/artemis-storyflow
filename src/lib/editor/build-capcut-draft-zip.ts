/**
 * @deprecated Use `buildCapcutExport` from `@/lib/editor/capcut-export`.
 * Mantido para imports existentes.
 */
export {
  buildCapcutDraftZip,
  buildCapcutExport,
  cleanupExpiredCapcutExports,
  openCapcutZipStream,
  resolveCapcutExportToken,
} from "@/lib/editor/capcut-export";
